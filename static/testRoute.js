// const { isLoaded as isAuthLoaded, load as loadAuth } from 'redux/modules/auth';

class App extends React.Component {
  render() {
    return (
        <div>So cool </div>
    )
  }
}

ReactDOM.render(React.createElement(App), document.getElementById('entry'))